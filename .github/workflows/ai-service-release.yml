name: ai-service release

on:
  push:
    branches: [main]
    paths:
      - 'ai-service/**'
      - 'common/**'
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event_name != 'release' || startsWith(github.event.release.tag_name, 'ai-service-v')
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      contents: write
    outputs:
      version: ${{ steps.bump.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "beka birhanu"
          git config user.email "beka.b.atomsa@gmail.com"

      - name: Get or bump version
        id: bump
        run: |
          if [[ "${{ github.event_name }}" == "release" ]]; then
            TAG="${{ github.event.release.tag_name }}"
            NEXT="${TAG#ai-service-v}"
          else
            git fetch --tags
            LATEST=$(git tag | grep "^ai-service-v0\.0\." | sort -t. -k3 -n | tail -1)
            if [ -z "$LATEST" ]; then
              X=0
            else
              X=$(echo "$LATEST" | sed 's/ai-service-v0\.0\.//')
            fi
            NEXT="0.0.$((X+1))"
            git tag "ai-service-v${NEXT}"
            git push origin "ai-service-v${NEXT}"
          fi
          echo "version=$NEXT" >> $GITHUB_OUTPUT

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ai-service/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-service:v${{ steps.bump.outputs.version }}
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-service:latest

      - name: Update deployment image tag
        run: |
          VERSION="v${{ steps.bump.outputs.version }}"
          sed -i "s|image: .*/ai-service:.*|image: ${{ secrets.DOCKERHUB_USERNAME }}/ai-service:${VERSION}|" \
            infra/ai-service/deployment.yaml
          git add infra/ai-service/deployment.yaml
          git diff --staged --quiet || git commit -m "chore(deploy): bump ai-service to ${VERSION} [skip ci]"
          git push origin HEAD:main
