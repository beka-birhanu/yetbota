package constants

const (
	DefaultPaginationLength = 15
)

// Chunking defaults for RAG ingestion
const (
	DefaultChunkSize    = 512
	DefaultChunkOverlap = 64
)

// RAG retrieval defaults
const (
	DefaultTopK = 5
)