# Post Processing

- Compress photos to a different resolution as per database design (mobile, web).
- Trigger child-workflow that is handled by the AI service to vectorize the post contents and link the post to other similar posts in the graph DB.
- Trigger feed update workflow to update the feeds of interested users with the new post.
