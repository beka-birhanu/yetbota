apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: yetbota-infra
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: yetbota
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
